=== Fix 5: get_candles _resolve_futures ===
FOUND at idx 160049:  from_ts = to_ts - (200 * 15 * 60)
                        inst_key = self._resolve_futures(inst)
                        mstock_candles =

=== Fix 4: WAITING in add_position ===
FOUND at idx 142709:      "direction": "WAITING",   # explicit so dashboard shows "WAITING" not "—"
                    "side": "WAITING",
                    "strategy": data.get("strategy", "SAR_TOP_BOTTOM"),
                    "sector": sector,
                    "initial_lots": initial_lots,
                 

=== Fix 3: base_sym derivation in _build_state ===
FOUND at idx 101112: e symbol from full contract name (e.g. BHELSEPFUT26 → BHEL)
            base_sym = inst
            for suf in ("FUT", "FUTURES"):
                base_sym = base_sym.replace(suf, "")
            base_sym = re.sub(r"(JAN|FEB|MAR|APR|MAY|JUN|JUL|AUG|SEP|OCT|NOV|DEC)\d{0,2}\d{2}$", "", base_sym)
            base_sym = re.sub(r"^\d+(JAN|FEB|MAR|APR|MAY|JUN|JUL|AUG|SEP|OCT|NOV|DEC)", "", base_sym

=== Fix 1: sectorMap OTHER ===
FOUND at idx 30860

=== Fix 2: direction WAITING ===
FOUND at idx 31713

=== sectorLabel logic ===
→ 'Other'
    const rawSector=p.sector;
    const sectorLabel=!rawSector?'—':(sectorMap[rawSector]||(rawSector==='OTHER'?'Other':rawSector));
    // Pyramiding lots: DB uses pyramid_lots, engine uses pyramids
    const pyrLots=p.pyramiding_lots||p.py
